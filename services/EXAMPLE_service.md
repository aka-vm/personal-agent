# <Service Name>

One-line description of what this service is and what the agent can do with it.

## CLI
```bash
python3 tools/<tool>.py <command> [args]   # what it does
python3 tools/<tool>.py search "<query>"   # another example
```

## Accounts / Scope
- account-a → what it maps to
- account-b → what it maps to

## Credentials
- Where tokens/keys live (paths, NOT the secrets themselves), e.g.
  ~/.config/<service>/token.json
- How to (re)authenticate if it expires.

## Notes
- Gotchas, formats, rate limits, anything the agent should know before acting.
