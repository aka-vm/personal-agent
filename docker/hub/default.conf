# :80 — the dashboard (control UI + /api proxy). Never exposed publicly.
server {
    listen 80 default_server;
    location /api/ {
        proxy_pass http://127.0.0.1:8090/api/;
    }
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
